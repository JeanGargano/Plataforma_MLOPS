apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert --file docker-compose.yml --out k8s/ --volumes hostPath
    kompose.version: 1.32.0 (765fde254)
  labels:
    io.kompose.service: mlflow-server
  name: mlflow-server
spec:
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
  selector:
    io.kompose.service: mlflow-server
